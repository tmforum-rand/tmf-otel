# TM Forum ODA Assets Collection

Asset collection of current, stable assets from the TM Forum ODA (Open Digital Architecture) project.

## Components

The `components/` directory holds the current collection of stable component definitions.

## Open APIs

The `apis/` directory holds the currently-available stable (v4.x and v5.x) OpenAPI/Swagger files.

